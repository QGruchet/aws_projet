version: '3.9'
services:
  back:
    build: .
    container_name: app
    depends_on:
      - db
    links:
      - db
    ports:
      - 80:80
    restart: always
    volumes:
      - ./app:/app
    command: sh -c 'npm i install -g && npm start dev'

  db:
    image: postgres:alpine
    container_name: db
    env_file:
      - config/db.env
    ports:
      - 5432:5432
    restart: always
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
