import React from 'react';
import logo from "../logo.png"

const Logo = () => {
    return (
        <div>
            <img src={logo} alt={"Cannot load logo"}/>
        </div>
    );
};

export default Logo;